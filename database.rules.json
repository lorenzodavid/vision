{
  "rules": {
    "talents" : {
      "$tid" : {
      	".read" : "root.child('talents').child($tid).child('groups').hasChildren([root.child('users').child(auth.uid).child('group').val()])"
      }
    }
  }
}